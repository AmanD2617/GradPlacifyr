-- Creates the placement_portal database.
--
-- HOW TO USE:
-- 1. Connect to your PostgreSQL server (use the default "postgres" database)
-- 2. Run this script to create the database
-- 3. Then switch your connection to "placement_portal"
-- 4. Run init-db.sql to create tables and types
--
-- NOTE: If the database already exists, this will throw a harmless error.
--       You can safely ignore it and proceed to step 3.

CREATE DATABASE placement_portal;
